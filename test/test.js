var should = require('chai').should();

describe('Highlight', () => {
    describe('#fromJson'), () => {
        it ("Should create a Highlighted instance from a JSON string", () => {
            let json = 
        });
    };
});

